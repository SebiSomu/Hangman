<Window x:Class="Hangman.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hangman"
        xmlns:converters="clr-namespace:Hangman.Converters"
        mc:Ignorable="d"
        Title="Hangman - Spânzurătoarea" 
        Height="700" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="White"
        FontFamily="Segoe UI, Inter, sans-serif">
    
    <Window.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding NewGameCommand}"/>
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding OpenGameCommand}"/>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveCurrentGameCommand}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- WPF Menu Bar (visible after login) -->
        <Menu Grid.Row="0" 
              Visibility="{Binding IsMenuVisible, Converter={StaticResource BoolToVisibilityConverter}}"
              Background="#F9FAFB" 
              FontSize="14"
              Padding="8,4">
            <MenuItem Header="_File">
                <MenuItem Header="_New Game" Command="{Binding NewGameCommand}" InputGestureText="Ctrl+N"/>
                <MenuItem Header="_Open Game" Command="{Binding OpenGameCommand}" InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Save Game" Command="{Binding SaveCurrentGameCommand}" InputGestureText="Ctrl+S"/>
                <Separator/>
                <MenuItem Header="S_tatistics" Command="{Binding ShowStatisticsCommand}"/>
                <Separator/>
                <MenuItem Header="_Cancel" Command="{Binding CancelCommand}"/>
            </MenuItem>
            <MenuItem Header="_Categories" ItemsSource="{Binding Categories}">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Setter Property="Header" Value="{Binding}"/>
                        <Setter Property="Command" Value="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        <Setter Property="CommandParameter" Value="{Binding}"/>
                    </Style>
                </MenuItem.ItemContainerStyle>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Command="{Binding AboutCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Main Content -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
