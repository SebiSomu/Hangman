<UserControl x:Class="Hangman.Views.CategoriesManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:Hangman.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White">
    
    <UserControl.Resources>
        <converters:ObjectToBooleanConverter x:Key="ObjectToBooleanConverter"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#6366F1"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="BackgroundSubtleColor" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryActionButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource BorderColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource DangerColor}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" 
                       Text="Manage Categories" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryColor}"/>
            
            <Button Grid.Column="1" 
                    Style="{StaticResource SecondaryActionButton}" 
                    Content="Back"
                    Command="{Binding BackCommand}"/>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Categories List -->
            <Border Grid.Column="0" 
                    Background="{StaticResource BackgroundSubtleColor}" 
                    CornerRadius="12"
                    Padding="15"
                    Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="Categories" 
                               FontSize="18" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryColor}"
                               Margin="0,0,0,10"/>
                    
                    <!-- Add Category -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="150"
                                 Height="36"
                                 Margin="0,0,10,0"
                                 FontSize="14"
                                 VerticalContentAlignment="Center">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <Button Style="{StaticResource ActionButton}" 
                                Content="Add"
                                Command="{Binding AddCategoryCommand}"/>
                    </StackPanel>
                    
                    <!-- Categories -->
                    <ListBox Grid.Row="2" 
                             ItemsSource="{Binding Categories}" 
                             SelectedItem="{Binding SelectedCategory}"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" 
                                        CornerRadius="8" 
                                        Padding="12,8"
                                        Margin="0,2">
                                    <TextBlock Text="{Binding Name}" 
                                               FontSize="14" 
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryColor}"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <Button Grid.Row="3" 
                            Style="{StaticResource DangerButton}" 
                            Content="Delete Selected Category"
                            Command="{Binding DeleteCategoryCommand}"
                            Margin="0,10,0,0"/>
                </Grid>
            </Border>

            <!-- Words List -->
            <Border Grid.Column="1" 
                    Background="{StaticResource BackgroundSubtleColor}" 
                    CornerRadius="12"
                    Padding="15"
                    Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="{Binding SelectedCategory.Name, FallbackValue='Select a category', StringFormat='{}{0} Words'}" 
                               FontSize="18" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryColor}"
                               Margin="0,0,0,10"/>
                    
                    <!-- Add Word -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox Text="{Binding NewWord, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="150"
                                 Height="36"
                                 Margin="0,0,10,0"
                                 FontSize="14"
                                 VerticalContentAlignment="Center">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <Button Style="{StaticResource ActionButton}" 
                                Content="Add Word"
                                Command="{Binding AddWordCommand}"/>
                    </StackPanel>
                    
                    <!-- Words -->
                    <ListBox Grid.Row="2" 
                             ItemsSource="{Binding SelectedCategory.Words}" 
                             SelectedItem="{Binding SelectedCategory.SelectedWord}"
                             BorderThickness="0"
                             Background="Transparent"
                             IsEnabled="{Binding SelectedCategory, Converter={StaticResource ObjectToBooleanConverter}}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" 
                                        CornerRadius="8" 
                                        Padding="12,8"
                                        Margin="0,2">
                                    <TextBlock Text="{Binding}" 
                                               FontSize="14"
                                               Foreground="{StaticResource TextPrimaryColor}"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <Button Grid.Row="3" 
                            Style="{StaticResource DangerButton}" 
                            Content="Delete Selected Word"
                            Command="{Binding DeleteWordCommand}"
                            Margin="0,10,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
