<UserControl x:Class="Hangman.Views.PlayerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:Hangman.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600"
             Background="White">
    
    <UserControl.Resources>
        <converters:StringToNullVisibilityConverter x:Key="StringToNullVisibilityConverter"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#6366F1"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="BackgroundSubtleColor" Color="#F9FAFB"/>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="24,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource BorderColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Player Settings" 
                       FontSize="32" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryColor}" 
                       Margin="0,0,0,8"/>
            <TextBlock Text="Customize your profile" 
                       FontSize="15" 
                       Foreground="{StaticResource TextSecondaryColor}"/> 
        </StackPanel>

        <!-- Content -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <!-- Profile Picture -->
            <Border Width="150" Height="150"
                    CornerRadius="75"
                    Background="{StaticResource BackgroundSubtleColor}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="2"
                    Margin="0,0,0,20">
                <Grid>
                    <materialDesign:PackIcon Kind="AccountCircleOutline" Width="60" Height="60"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryColor}"
                               Visibility="{Binding AvatarImage, Converter={StaticResource StringToNullVisibilityConverter}}"/>
                    <Ellipse Width="146" Height="146">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding AvatarImage}" Stretch="UniformToFill"/>
                        </Ellipse.Fill>
                    </Ellipse>
                </Grid>
            </Border>

            <Button Style="{StaticResource ModernButton}" 
                    Content="Change Profile Picture" 
                    Command="{Binding ChangeImageCommand}"
                    Margin="0,0,0,20"/>

            <!-- Username Display -->
            <TextBlock Text="{Binding CurrentUser.Username, StringFormat='Username: {0}'}" 
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryColor}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            <Button Style="{StaticResource ModernButton}" 
                    Content="Save Changes" 
                    Command="{Binding SaveCommand}"
                    Margin="0,0,12,0"/>
            <Button Style="{StaticResource SecondaryButton}" 
                    Content="← Back" 
                    Command="{Binding BackCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
