<UserControl x:Class="Hangman.Views.SettingsHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White">
    
    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#6366F1"/>
        <SolidColorBrush x:Key="PrimaryHoverColor" Color="#4F46E5"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="BackgroundSubtleColor" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="CardHoverColor" Color="#F3F4F6"/>

        <Style x:Key="SettingsCard" TargetType="Button">
            <Setter Property="Width" Value="280"/>
            <Setter Property="Height" Value="200"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="12" Opacity="0.1" Color="#000000"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource CardHoverColor}"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform Y="-6"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="24,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="Background" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundSubtleColor}" Padding="32,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- User Profile -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Width="48" Height="48" CornerRadius="24" Background="{StaticResource PrimaryColor}" Margin="0,0,16,0">
                        <Ellipse Width="46" Height="46">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{Binding AvatarImage}" Stretch="UniformToFill"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Settings" FontSize="24" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryColor}"/>
                        <TextBlock Text="{Binding WelcomeMessage}" FontSize="14"
                                   Foreground="{StaticResource TextSecondaryColor}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <!-- Settings Cards -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,40">
                    <!-- Edit Profile Card -->
                    <Button Style="{StaticResource SettingsCard}"
                            Command="{Binding EditProfileCommand}">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="AccountEditOutline" Width="56" Height="56" HorizontalAlignment="Center" Margin="0,0,0,16" Foreground="{StaticResource PrimaryColor}"/>
                            <TextBlock Text="Edit Profile" FontSize="20" FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryColor}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Change avatar and username" FontSize="14" 
                                       Foreground="{StaticResource TextSecondaryColor}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Button>

                    <!-- Edit Word Repository Card -->
                    <Button Style="{StaticResource SettingsCard}"
                            Command="{Binding EditWordRepositoryCommand}">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="BookEditOutline" Width="56" Height="56" HorizontalAlignment="Center" Margin="0,0,0,16" Foreground="{StaticResource PrimaryColor}"/>
                            <TextBlock Text="Edit Word Repository" FontSize="20" FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryColor}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Manage categories and words" FontSize="14" 
                                       Foreground="{StaticResource TextSecondaryColor}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Back Button -->
                <Button Style="{StaticResource SecondaryButton}" 
                        Content="Back to Main Menu" 
                        Command="{Binding BackCommand}"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
